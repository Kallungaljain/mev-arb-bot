{
  "name": "elite-ant-arb-contracts",
  "version": "1.0.0",
  "description": "Oracle-free flash loan arbitrage contracts for Polygon",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:polygon": "hardhat run scripts/deploy.ts --network polygon",
    "deploy:mumbai": "hardhat run scripts/deploy.ts --network mumbai",
    "verify": "hardhat verify --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
